#if !defined(HEXADECIMAL_H)
#define HEXADECIMAL_H
#include <cctype>
#include <cmath>
#include <string>

namespace hexadecimal {
    int convert(const std::string& input);
}  // namespace hexadecimal

#endif // HEXADECIMAL_H